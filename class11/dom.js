// ==================================================================
// DOM - Document Object Model

/**
 * niezależny od języka interfejs do pracy z dokumentami HTML
 * polaczenie "strony" z javascriptem
 * DOM - reprezentacja dokumentu HTML
 * ustandaryzowane (w kazdej przegladarce takie same)
 */

// ==================================================================
// BOM - Browser Object Model

/**
 * obiekty udostępniane przez przeglądarke (location, window, navigator)
 * brak standaryzacji (kazda przegladarka moze miec rozna implementacje)
 */

// ==================================================================
// Dokument HTML

/**
 * Każdy element html tworzy osobny element (Node), każdy tekst element tekstowy (TextNode)
 * struktura drzewa, każdy element może mieć tylko jednego rodzica
 * HTML parser
 * Budowany stopniowo, dodawanie nodeów gdy tylko się pojawią
 * DOMContentLoaded
 * DOM Tree
 */
